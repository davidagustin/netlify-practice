---
import { Marked } from 'marked';

interface Props {
    content: string;
    class?: string;
}

const { content, class: className } = Astro.props;

const html = new Marked().parse(content);
---

<div class:list={['markdown', className]} set:html={html} />
